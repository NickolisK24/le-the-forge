"""
Game data package — structured JSON files for Last Epoch game constants.

Files:
  classes.json  — base stats, mastery bonuses, keystone bonuses, attribute scaling
  affixes.json  — affix tier midpoints and stat_key mappings
  skills.json   — skill stat definitions (base damage, level scaling, scaling stats)

Use game_data_loader.py to load and cache these at import time.
"""
